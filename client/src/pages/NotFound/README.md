# 📄 Page Not Found (404) – React Component

This component represents a 404 – Page Not Found screen in a React application.
It is shown when a user tries to access a route that does not exist.

---

## Component Overview

A simple functional React component written using JSX.

### Purpose

- Inform users that the requested page does not exist
- Provide a clean and centered layout
- Serve as a fallback route in routing systems like React Router

---

## Concepts Used

- React Functional Component
- JSX (JavaScript XML) syntax
- Single Parent Rule in JSX
- JavaScript expressions inside JSX (`{}`)
- Conditional rendering using ternary operator
- Dynamic values (dates, calculations, random numbers)
- Flexbox layout using Tailwind CSS
- Tailwind CSS utility-first styling
- Block-level container elements (`div`)
- Basic component structure and readability best practices
